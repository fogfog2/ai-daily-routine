# 클라우드에서 루틴 돌리기 (PC 꺼도 됨)

https://claude.ai/code/routines 에 등록하면 PC 전원과 무관하게 실행됩니다.

---

## 등록

**Create routine** → Repository 는 **`fogfog2/ai-daily-routine`**.

프롬프트는 **한 줄이면 충분합니다.**

```
ai-daily-routine 저장소의 ROUTINE.md 를 읽고 그대로 수행해줘.
```

`ROUTINE.md` 가 요일을 보고 무엇을 할지 스스로 정합니다 —
평일은 뉴스 수집과 판정만, 토요일은 문서 반영까지.

### 권장 스케줄

| 시각 | 하는 일 |
|---|---|
| 매일 오전 9:37 | 뉴스 수집 → 문서 영향 판정 |

토요일 반영까지 한 프롬프트가 처리하므로 **루틴은 하나만 만들면 됩니다.**
따로 나누고 싶으면 토요일 것을 하나 더 만들되 프롬프트는 같아도 됩니다.

---

## 루틴을 바꾸고 싶을 때

**`ROUTINE.md` 만 고치면 됩니다.** routines 설정도, 스킬도, 스크립트도 건드릴 필요 없습니다.

```bash
# 고치고
git add ROUTINE.md && git commit -m "루틴 수정" && git push
```

다음 실행부터 반영됩니다.

---

## 저장소 두 개의 역할

| 저장소 | 내용 | 누가 쓰나 |
|---|---|---|
| `fogfog2/ai-daily-routine` | 스킬 · 카탈로그 · 생성기 · 스크립트 | 루틴 |
| `fogfog2/ai-concepts` | 완성된 HTML 49편 (GitHub Pages) | 방문자 |

루틴은 자기 저장소에서 돌다가, 배포할 때만 사이트 저장소를 clone 해서 푸시합니다.
`ship-docs.sh` 가 자동으로 합니다.

> **두 저장소 모두 Public 입니다.** 로컬 경로·내부 IP·키를 넣지 마세요.
> `ship-docs.sh` 5단계가 검사하지만, 애초에 쓰지 않는 것이 안전합니다.

---

## 클라우드에서 달라지는 것

없습니다. 배포 대상이 **GitHub Pages 하나뿐**이라 집 PC 와 동일하게 동작합니다.

| 항목 | 처리 |
|---|---|
| `site-pages/` 가 없음 | `ship-docs.sh` 가 자동 clone |
| GitHub Pages 배포 | 정상 동작 (푸시 권한만 있으면 됨) |

집 서버·로컬 서버·Tailscale·앱 내장 사본은 **더 이상 쓰지 않습니다.**

---

## 푸시 권한

사이트 저장소(`ai-concepts`)는 루틴 저장소와 **다른 저장소**라 별도 권한이 필요할 수 있습니다.

푸시가 막히면 `ship-docs.sh` 는 **검증까지만 하고 멈춥니다.**
잘못된 내용이 올라가는 일은 없으니, 실패하면 그때 조정하면 됩니다.
