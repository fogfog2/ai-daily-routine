# ROUTINE — 이 파일 하나만 읽고 수행한다

> **Claude 에게.** 이 파일이 유일한 진입점이다. 여기 적힌 대로만 하면 된다.
>
> **사용자에게.** 루틴을 바꾸려면 **이 파일만 고치면 된다.**

---

## 매일 두 가지를 한다

서로 독립이다. 한쪽이 부실해도 다른 쪽은 그대로 수행한다.

| | 하는 일 | 결과물 |
|---|---|---|
| **[A] 데일리 뉴스** | 최근 24시간 AI 소식을 모아 정리 | `archive/YYYY-MM-DD.md` |
| **[B] 기술 문서** | **매일 1편** 새로 작성 | `site-pages/docs/<slug>.html` |

마지막에 **[C] 배포**로 둘 다 사이트에 올린다.

> **[B]는 매일 새 문서를 쓴다.** 기존 문서가 낡았는지 판정하는 일은 하지 않는다.
> 대신 **기존 문서가 언급했지만 설명이 부족한 것**을 찾아 그것을 쓴다.

배포 대상은 **GitHub Pages 하나뿐이다.** https://fogfog2.github.io/ai-concepts/
집 서버·로컬 서버·Tailscale 은 쓰지 않는다.

---

## [A] 데일리 뉴스

가볍게 한다. 여기서 시간을 많이 쓰지 않는다.

### 0. 어제까지 무엇을 썼는지 먼저 본다 — 건너뛰지 말 것

검색 **전에** 최근 기록을 읽는다. 이걸 빼먹어서
2026-08-12 에 4건 중 3건이 전날과 겹친 적이 있다.

```bash
head -60 archive/$(date -d yesterday +%F).md 2>/dev/null | grep -E "^## |^- 출처"
grep -h "^## " archive/*.md | tail -30      # 최근 제목 훑기
```

여기 **이미 있는 사건은 다시 쓰지 않는다.** 후속 보도가 나왔더라도
*새로운 사실*(숫자·날짜·결정)이 없으면 새 항목이 아니다.

### 1. 검색

**오늘 날짜에 일어난 일만 쓴다.**

- 기준일은 `date +%F` 로 확인한 **오늘**이다
- 어제 일어난 사건은 어제 기록에 들어갔어야 한다. **오늘 끌어오지 않는다**
- 오늘 처음 공개됐다면 사건 자체가 며칠 전이어도 된다
  (예: 논문 게재일은 지났지만 오늘 공개된 벤치마크 결과)
- 애매하면 **원문의 게시 시각**으로 판단한다

검색어에 날짜를 넣어 좁힌다. 광범위한 "August 2026" 류는
지난주 것을 끌어오므로 쓰지 않는다.

**서로 다른 4개 이상 각도**로 찾는다. AI 전 분야를 본다 —
에이전트·코딩 도구에 쏠리지 않게 한다.

| 축 | 예 |
|---|---|
| 회사·제품 | Anthropic, OpenAI, Google DeepMind, Meta, Mistral, Qwen, DeepSeek |
| **비전** | 검출·분할, 생성 영상, 3D·NeRF·가우시안, 의료영상, 자율주행 인식 |
| **음성·오디오** | ASR, TTS, 음악 생성, 실시간 번역 |
| **과학·바이오** | 단백질 구조, 신약, 재료, 기상, 수학 |
| **로보틱스·체화** | VLA 모델, 조작, 시뮬레이션 |
| 시스템·하드웨어 | 가속기, 추론 최적화, 양자화, 커널, 데이터센터 |
| 연구 흐름 | 새 아키텍처, 학습 기법, 벤치마크, 재현·반박 |
| 사회·제도 | 규제, 소송, 저작권, 안전, 노동 영향 |
| 오픈소스 | 가중치 공개, 라이선스 변경, 커뮤니티 재현 |

> **쏠림 방지.** 한 회차에서 **`산업`(인사·투자·조직) 은 2건을 넘기지 않는다.**
> 사람이 옮기고 돈이 오간 소식은 검색에 많이 잡히지만 대개 6개월 뒤에 남지 않는다.
> **연구·모델·비전 쪽을 우선한다.**

### 2. 선별 — 3~6건

| 기준 | 배점 |
|---|---|
| 실사용 영향 | 3 |
| 신규성 (오늘 처음 나온 사실인가) | 2 |
| 출처 신뢰도 (1차 출처인가) | 2 |
| 지속성 | 1 |

중복 사건은 가장 1차 출처에 가까운 것만.
**후보가 적으면 적은 대로 낸다. 숫자를 채우지 않는다.**
2건뿐이면 2건만 쓴다 — 전날 것을 끌어와 채우는 것보다 낫다.

### 3. 확인

선별한 것은 **WebFetch 로 원문을 연다.** 제목만 보고 요약하지 않는다.
열지 못하면 `(미확인)` 이라고 적는다.

### 4. 기록

`archive/YYYY-MM-DD.md` 에 **아래 형식 그대로** 쓴다.
이 형식을 `build-news.py` 가 파싱하므로 어기면 사이트에 안 올라간다.

```markdown
# AI Daily — 2026-08-11

## 항목 제목

- 분류: `모델`
- 출처: https://example.com/...

사실 한 줄. 숫자·버전·날짜를 포함한다.
사실 두 줄.
사실 세 줄.

그래서: 한 줄 해석. 사실과 분리한다.
```

분류는 아래 중 하나. **새로 만들지 않는다** — 필터가 어긋난다.

| 분류 | 쓰는 경우 |
|---|---|
| `모델` | 모델·가중치 공개, 스펙 변경 |
| `연구` | 논문, 새 기법, 벤치마크, 재현·반박 |
| `비전` | 이미지·영상·3D 인식과 생성 |
| `음성` | ASR·TTS·오디오·음악 |
| `과학` | 바이오·재료·기상·수학 등 과학 응용 |
| `로보틱스` | 체화 AI, VLA, 조작·주행 |
| `시스템` | 하드웨어, 추론 최적화, 인프라 |
| `도구` | 개발 도구, 프레임워크, 제품 기능 |
| `산업` | 인사·투자·조직 (**회차당 2건 이하**) |
| `규제` | 법·정책·소송·표준 |

한 사건이 둘에 걸치면 **더 좁은 쪽**을 고른다.
비전 모델 공개는 `모델` 이 아니라 `비전` 이다.

---

## [B] 기술 문서 — 매일 1편

### 1. 무엇을 쓸지 정한다

**우선순위대로** 찾는다.

**① 기존 문서가 언급했지만 따로 설명하지 않은 것** ← 가장 좋은 후보

기존 49편을 읽다 보면 "여기서 자세히 다루지 않는다", "별도 주제다" 하고
넘어간 개념들이 있다. 그것이 곧 결손이다.

```bash
grep -rn "다루지 않는다\|별도\|생략\|여기서는" site-pages/docs/*.html | head -20
```

**② `DOCS-BACKLOG.md` 의 후보 목록**

미리 정리해 둔 결손 목록이 있다. 거기서 고른다.

**③ 오늘 뉴스에서 흐름이 보이는 주제**

[A]에서 모은 소식 중 **6개월 뒤에도 유효한** 기반 기술이면 후보가 된다.
단일 제품 릴리스는 아니다.

### 태그로 균형을 본다 — 고르기 전에

문서마다 태그가 붙어 있어 **어디가 비었는지 숫자로 보인다.**

```bash
python3 - <<'PY'
import sys; sys.path.insert(0, "scripts")
from catalog import DOCS, TAGS
from collections import Counter
c = Counter(t for d in DOCS for t in d[7])
for g in ("domain", "topic"):
    print(g)
    for t in TAGS:
        if t["group"] == g: print(f"   {t['name']:10} {c[t['id']]:2}편")
PY
```

**고르는 순서**

1. **0편이거나 관심사 대비 얇은 태그**를 먼저 채운다
2. **vision 을 우선한다** — 사용자의 관심 영역이고, 현재 LLM 보다 적다
3. 관심사 태그 넷을 특히 챙긴다 — `detection` `on-device` `enhance` `feature`
4. 같은 태그가 **연달아 3편 이상** 나오지 않게 한다. 한쪽으로 쏠리면 흐름이 끊긴다

### 태그를 붙인다

새 문서를 등록할 때 태그도 함께 정한다.

- **도메인 1개** — `llm` `vision` `common` 중 하나 (반드시 하나)
- **주제 1~3개** — `detection` `on-device` `enhance` `feature` `generative`
  `arch` `training` `alignment` `efficiency` `systems` `safety`

태그가 틀리면 필터가 어긋나므로 `catalog.py` 의 `TAGS` 설명을 보고 정확히 고른다.
검증이 미정의 태그와 도메인 개수를 잡아준다.

### 2. 중복 확인 — 반드시

이미 있는 내용을 또 쓰면 안 된다.

```bash
python3 scripts/catalog.py                    # 단계별 편수
grep -n '", "' scripts/catalog.py | head -60   # 전체 제목·slug
```

기존 문서에 **한 절 덧붙이면 되는 정도라면 새 문서를 만들지 않는다.**
그 문서의 생성기를 고쳐 절을 추가한다.

### 3. 사실 확인 — 건너뛰지 말 것

- 수치는 `python3 -c` 로 **직접 계산**한다
- 논문 주장은 WebFetch 로 원문을 연다
- 자주 바뀌는 주제(검출기 SOTA, 모델 스펙)는 **웹으로 재확인**한다
- **출처가 어긋나면 그 사실을 문서에 적는다.** 한쪽을 골라 단정하지 않는다

> 기억으로 썼다가 틀린 전례가 있다 — RF-DETR 수치, speculative decoding 식,
> 증류 문서의 온도 softmax 값. 전부 계산·확인에서 잡혔다.

### 4. 작성

`scripts/docs/` 에서 기존 생성기 하나를 본떠 만든다.
**HTML 을 직접 쓰지 않는다.** 다음 빌드에 덮어써진다.

```bash
cd scripts/docs
cp gen_vae.py gen_<주제>.py     # 비슷한 편을 본뜬다
# 팔레트·본문·읽을거리를 고친다
python3 gen_<주제>.py
python3 check.py ../../site-pages/docs/<slug>.html
```

`scripts/catalog.py` 의 `DOCS` 에 등록한다.

```python
("제목", "부제", "", "slug", "단계id", "한 줄 요약", "2026-08-11",
 ["vision", "enhance"]),
#  ↑ uuid 는 빈 문자열      ↑ 도메인 1개 + 주제 1~3개
```

**포맷은 기존 49편과 같아야 한다** — 번호 절 · `.eq` 수식 블록 ·
손으로 짠 SVG + `figcaption` · `.note` 강조 · arXiv 인용 5~6개 ·
편별 팔레트(라이트/다크 양쪽) · 평서체.
분량은 5개 절, 20,000자 내외.

### 5. 연결 — 이것이 핵심이다

문서를 낱개로 두지 않는다. **전체가 하나의 흐름으로 읽혀야 한다.**

**새 문서에서 기존 문서로**

설명에 필요한 선행 개념이 이미 문서로 있으면 링크한다.

```html
<a href="autoencoders-vae.html">VAE</a>에서 봤듯 …
```

**기존 문서에서 새 문서로** ← 자주 빠뜨리는 쪽

새 문서가 다루는 개념을 **기존 문서가 언급만 하고 지나갔다면**,
그 문서의 생성기를 열어 링크를 건다. 양방향으로 이어야 흐름이 생긴다.

```bash
grep -rln "<주제 키워드>" scripts/docs/gen_*.py
```

**링크는 같은 폴더 기준 상대 경로**다 (`href="slug.html"`).
`ship-docs.sh` 4단계가 깨진 링크를 잡는다.

---

## [C] 배포

```bash
./scripts/ship-docs.sh "메시지"
```

검증 5단계를 전부 통과해야 커밋한다.
**실패하면 고치고 다시 돌린다. 건너뛰지 않는다.**

그리고 루틴 저장소도 커밋한다.

```bash
git add -A && git commit -m "요약" && git push
```

`RUNLOG.md` 에 한 줄 추가한다.

---

## 보고

- **뉴스**: 선별 건수와 분류 구성, 주목할 항목 1~2개
- **문서**: 무엇을 왜 골랐는지, 어느 문서와 연결했는지, 배포 URL
- **사실 확인에서 틀린 것을 잡았으면 반드시 명시한다**
- 확인 실패·건너뛴 항목

> 뉴스가 적으면 적은 대로 말한다. 채워 넣지 않는다.
> 다만 **문서 1편은 매일 쓴다** — 쓸 것이 없다는 결론은 나오지 않아야 한다.
> 결손 후보가 정말 떨어졌다면 그 사실을 보고하고 `DOCS-BACKLOG.md` 를 채울 것을 제안한다.

---

## 원칙

1. **수치는 기억으로 쓰지 않는다.** 계산하거나 1차 출처로 확인한다.
2. **기존 문서와 겹치면 새로 쓰지 않는다.** 그 문서를 늘린다.
3. **양방향으로 링크한다.** 새 문서만 링크를 걸고 끝내지 않는다.
4. **검증을 통과하지 못하면 배포하지 않는다.**
5. **이 저장소는 공개다.** 로컬 경로·내부 IP·키가 들어가면 안 된다.
6. **품질 기준을 낮추지 않는다.** 매일 쓴다고 얕아지면 안 된다.
