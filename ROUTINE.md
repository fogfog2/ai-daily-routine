# ROUTINE — 이 파일 하나만 읽고 수행한다

> **Claude 에게.** 이 파일이 유일한 진입점이다. 여기 적힌 대로만 하면 된다.
> 다른 문서를 먼저 찾아 읽을 필요 없다. 필요한 시점에 이 파일이 가리킨다.
>
> **사용자에게.** 루틴을 바꾸고 싶으면 **이 파일만 고치면 된다.**
> 스킬·스크립트는 건드리지 않아도 된다.

---

## 오늘 무엇을 할지 정하기

요일을 확인해서 아래를 고른다. 사용자가 특정 작업을 지정했으면 그것을 우선한다.

| 조건 | 수행할 것 |
|---|---|
| 매일 | **[A] 뉴스 수집** → **[B] 문서 영향 판정** |
| 토요일 | 위에 더해 **[C] 문서 반영** |
| 대기열이 비어 있음 | [C] 는 건너뛴다. **아무것도 하지 않는 것이 정답이다.** |

---

## 배포 대상

**GitHub Pages 하나뿐이다.** https://fogfog2.github.io/ai-concepts/

집 서버·로컬 서버·Tailscale·앱 내장 사본은 **더 이상 쓰지 않는다.**
그런 경로가 언급된 오래된 문서를 보더라도 따르지 말 것.

---

## [A] 뉴스 수집

### 수집 대상 소스

| 구분 | 소스 | 비고 |
|---|---|---|
| 공식 발표 | Anthropic / OpenAI / Google DeepMind / Meta AI 블로그 | 1차 출처 우선 |
| 모델 릴리스 | 각 사 릴리스 노트, 모델 카드 | 버전·가격·한도 변화 |
| 연구 | arXiv cs.AI / cs.CL 주요 논문 | 인용·화제성 높은 것만 |
| 산업 | 주요 기술 매체 | 1차 출처 링크가 있는 것만 |
| 도구 | 개발자 도구 · 오픈소스 릴리스 | 실사용 영향 있는 것 |

> 원칙: **2차 기사보다 1차 출처.** 매체 기사를 골랐다면 원문 링크를 함께 남긴다.

### 1. 검색

최근 **24시간** 범위. 서로 다른 **3개 이상 각도**로 검색한다.

- 회사·제품명 — Anthropic, OpenAI, Google DeepMind, Meta AI
- 사건 유형 — model release, benchmark, AI regulation
- 도메인 — AI coding tools, multimodal, agents

산출물: 후보 링크 15~30건.

### 2. 선별

아래 점수로 **상위 5~8건**만 남긴다. 중복 사건은 가장 1차 출처에 가까운 것만.

| 기준 | 배점 | 설명 |
|---|---|---|
| 실사용 영향 | 3 | 내가 쓰는 도구·모델이 실제로 달라지는가 |
| 신규성 | 2 | 어제까지 없던 정보인가 |
| 출처 신뢰도 | 2 | 1차 출처인가 |
| 지속성 | 1 | 일주일 뒤에도 유효한가 |

> **후보가 적으면 적은 대로 낸다. 숫자를 채우지 않는다.**

### 3. 심층 확인

선별된 항목은 **WebFetch 로 원문을 연다.** 제목만 보고 요약하지 않는다.
열지 못한 항목은 `(미확인)` 으로 표시한다.

### 4. 기록

`archive/YYYY-MM-DD.md` 에 항목별로 쓴다.

```markdown
## 제목

- 분류: `모델` | `연구` | `도구` | `산업` | `규제`
- 출처: <URL>

사실 1 — 숫자·버전·날짜를 포함한다
사실 2
사실 3

그래서: 한 줄 해석. 사실과 분리한다.
```

파일이 없으면 만든다. 이미 오늘치가 있으면 이어서 쓴다.

---

## [B] 문서 영향 판정

방금 선별한 뉴스가 **기존 기술문서 49편을 낡게 만드는지** 본다.
여기서는 **판정만 한다. 문서를 고치지 않는다.**

### 판정 기준

**`갱신`** — 기존 문서를 고쳐야 하는 경우

- 문서에 적힌 **수치가 틀리게 됨** (예: "실시간 최초 60 AP" 를 넘는 모델 등장)
- 문서가 **현재 도달점**이라 서술한 것이 바뀜
- **라이선스·가용성**이 바뀜
- 문서의 주장이 후속 연구로 **반박됨**

**`신규`** — 새 문서가 필요한 경우

- 한 단계에 **관련 뉴스가 3건 이상** 쌓임 (산발적 언급이 아니라 흐름)
- 기존 49편 **어디에도 안 들어감** (겹치면 `신규` 가 아니라 `갱신`)
- **6개월 뒤에도 유효**한 내용

**넣지 않는 것**

- 단일 제품 릴리스·가격 변경·벤치마크 점수 → 뉴스로 충분
- 아직 논문 한 편뿐인 기법 → 흐름이 될 때까지 기다린다

### 기존 문서 확인

판정하려면 49편이 무엇을 다루는지 알아야 한다.

```bash
python3 scripts/catalog.py                   # 단계별 편수
grep -n '", "' scripts/catalog.py | head -60  # 제목·slug 목록
```

### 기록

해당하는 것만 `DOCS-BACKLOG.md` 대기열 표에 추가한다. **근거 URL 을 반드시 적는다.**
해당 없으면 표를 건드리지 않는다 — **대부분의 날이 여기에 해당한다.**

---

## [C] 문서 반영 (토요일)

### 1. 고르기

`DOCS-BACKLOG.md` 에서 우선순위 `높음` 부터 **1~2건만**.
`높음`(수치가 틀림·사실 오류)은 틀린 내용이 공개돼 있는 상태이므로 먼저 처리한다.

**대기열이 비었으면 여기서 끝낸다.** 억지로 주제를 만들지 않는다.

### 2. 사실 확인 — 건너뛰지 말 것

- 수치가 나오면 `python3 -c` 로 **직접 계산**한다
- 논문 주장은 WebFetch 로 원문을 연다
- 자주 바뀌는 주제(검출기 SOTA, 보안 권고, 모델 스펙)는 **웹으로 재확인**한다
- **출처가 서로 어긋나면 그 사실을 문서에 적는다.** 한쪽을 골라 단정하지 않는다

> 과거에 기억으로 썼다가 틀린 전례가 있다 — RF-DETR 수치, speculative decoding 식,
> 증류 문서의 온도 softmax 값. 전부 계산·확인 단계에서 잡혔다.

### 3. 작성

**문서 HTML 을 직접 편집하지 않는다.** 다음 빌드에 덮어써진다.
반드시 `scripts/docs/gen_<주제>.py` 를 고친다.

```bash
cd scripts/docs
python3 gen_lora.py                                 # 렌더
python3 check.py ../../site-pages/docs/lora.html    # 점검
```

`신규` 면 기존 생성기 하나를 본떠 만들고 `scripts/catalog.py` 의 `DOCS` 에 등록한다.

```python
("제목", "부제", "", "slug", "단계id", "한 줄 요약", "YYYY-MM-DD"),
#                 ↑ 직접 쓴 문서는 uuid 를 빈 문자열로
```

**포맷은 기존 49편과 같아야 한다** — 번호 절 · `.eq` 수식 블록 ·
손으로 짠 SVG + `figcaption` · `.note` 강조 · arXiv 인용 · 편별 팔레트 · 평서체.
팔레트는 편마다 다르게 잡되 라이트/다크 양쪽을 정의한다.

### 4. 배포

```bash
./scripts/ship-docs.sh "커밋 메시지"
```

검증 5단계(카탈로그 · 문서 점검 · 상호링크 · 유출 스캔)를 전부 통과해야 커밋한다.
**실패하면 고치고 다시 돌린다. 검증을 건너뛰지 않는다.**

### 5. 기록

- `DOCS-BACKLOG.md` 해당 줄을 `완료 YYYY-MM-DD` 로 바꾸고 커밋 해시를 적는다
- `RUNLOG.md` 에 한 줄 추가한다

---

## 마무리 — 커밋

루틴 저장소에도 변경사항을 커밋·푸시한다.

```bash
git add -A && git commit -m "요약" && git push
```

---

## 보고

사용자에게 아래를 보고한다.

- 뉴스 선별 건수와 분류 구성, 주목할 항목 1~2개
- 대기열에 추가한 것 (없으면 없다고)
- 문서를 고쳤으면 무엇을 왜 고쳤는지 + 배포 URL
- **사실 확인에서 틀린 것을 잡았으면 반드시 명시한다**
- 확인 실패·건너뛴 항목

> 건수가 적거나 아무것도 하지 않았으면 **그대로 말한다.**
> "오늘은 문서를 건드릴 만한 변화가 없었다" 는 정상적인 결과다. 채워 넣지 않는다.

---

## 원칙

1. **매일 문서를 쓰지 않는다.** 기술문서는 일간 주기의 대상이 아니다.
2. **수치는 기억으로 쓰지 않는다.** 계산하거나 1차 출처로 확인한다.
3. **검증을 통과하지 못하면 배포하지 않는다.**
4. **이 저장소는 공개다.** 로컬 경로·내부 IP·키가 들어가면 안 된다.
