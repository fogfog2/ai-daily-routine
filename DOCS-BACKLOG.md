# DOCS-BACKLOG — 기술문서 대기열

매일 뉴스를 훑으면서 **"기존 문서가 낡았는가 / 새 주제가 필요한가"** 만 판정해 여기 쌓습니다.
실제 작성·수정은 주 1회 이 목록에서 골라 진행합니다.

> **왜 매일 쓰지 않는가.** Transformer 문서의 내용은 어제 모델이 나왔다고 달라지지 않습니다.
> 기술문서는 일간 주기의 대상이 아니고, 매일 한 편씩 채우면 분량만 늘고 질이 떨어집니다.
> 일간으로 의미 있는 것은 **신호 수집**이고, 그 신호가 쌓여 임계에 닿았을 때만 문서를 건드립니다.

---

## 판정 기준

뉴스 한 건을 보고 아래에 해당할 때만 대기열에 넣습니다. 아니면 넣지 않습니다.

### A. 기존 문서 갱신 (`갱신`)

| 조건 | 예 |
|---|---|
| 문서에 적힌 **수치가 틀리게 됨** | "실시간 최초 60 AP" 를 넘는 검출기가 나옴 |
| 문서가 **현재 도달점**이라 서술한 것이 바뀜 | RF-DETR 자리를 다른 모델이 차지 |
| **라이선스·가용성**이 바뀜 | Apache 2.0 이던 모델이 조건부로 전환 |
| 문서가 **틀렸다고 밝혀짐** | 인용한 논문의 주장이 후속 연구로 반박됨 |

### B. 새 문서 (`신규`)

| 조건 | 설명 |
|---|---|
| **한 단계에 3건 이상** 관련 뉴스가 쌓임 | 산발적 언급이 아니라 흐름이 생긴 것 |
| 기존 49편 중 **어디에도 안 들어감** | 겹치면 새로 쓰지 말고 기존 문서를 늘림 |
| **6개월 뒤에도 유효**할 내용인가 | 특정 제품 릴리스는 문서 주제가 아님 |

### 넣지 않는 것

- 단일 제품 릴리스·가격 변경·벤치마크 점수 소식 → **뉴스로 충분**
- 아직 논문 한 편뿐인 기법 → 흐름이 될 때까지 기다림
- 기존 문서에 한 문장 덧붙이면 되는 것 → `갱신` 으로 잡되 우선순위 낮음

---

## 대기열

우선순위: `높음`(수치가 틀림·사실 오류) > `보통`(맥락 보강) > `낮음`(있으면 좋음)

| 추가일 | 종류 | 대상 | 근거 (출처) | 우선순위 | 상태 |
|---|---|---|---|---|---|
| 2026-08-11 | — | (루틴 최초 구성. 아직 수집 미실행) | — | — | — |

### 상태 값

- `대기` — 판정만 됨
- `작업중` — 이번 주 작업 대상
- `완료 YYYY-MM-DD` — 반영 후 커밋 해시 병기
- `보류` — 판단 보류 (사유 병기)

---

## 스케줄 재등록

Claude Code 의 cron 은 **세션에 종속**됩니다. 세션이 끝나면 사라지고, 남아 있어도 7일 뒤 만료됩니다.
반면 스킬(`.claude/skills/`)은 파일이라 계속 남습니다. 그래서 **루틴 자체는 영구적이고, 자동 실행만 재등록**하면 됩니다.

새 세션에서 아래 한 줄이면 복구됩니다.

```
ai-daily 와 ai-docs 루틴 스케줄 다시 등록해줘
```

수동으로 돌릴 때는 스케줄 없이도 언제든 됩니다.

```
ai-daily 실행해줘        # 뉴스 + 문서 영향 판정
기술문서 갱신해줘         # 대기열에서 골라 작성·배포
```

---

## 주간 반영 절차

1. 대기열에서 **우선순위 높음 우선**으로 1~2건 고른다. 없으면 **아무것도 하지 않는다.**
2. `갱신` 이면 해당 `scripts/docs/gen_<주제>.py` 를 수정, `신규` 면 새 생성기를 만든다.
3. 수치는 **반드시 직접 계산하거나 1차 출처로 확인**한다. 기억으로 쓰지 않는다.
4. `./scripts/ship-docs.sh "메시지"` 로 검증→배포.
5. 이 표의 상태를 `완료 <날짜>` 로 바꾸고 커밋 해시를 적는다.
