# DOCS-BACKLOG — 문서 후보 목록

**매일 1편**을 여기서 골라 씁니다. 절차는 [ROUTINE.md](ROUTINE.md) `[B]` 를 따릅니다.

> **고르는 원칙.** 가장 좋은 후보는 **기존 문서가 여러 번 언급했는데 전용 문서가 없는 것**입니다.
> 이미 맥락이 깔려 있어 링크로 이을 곳이 많고, 읽는 사람이 궁금해할 지점이기 때문입니다.
>
> **vision 분야를 우선합니다.**

---

## 후보를 찾는 법

기존 문서 본문에서 자주 나오는데 전용 문서가 없는 주제를 센다.

```bash
python3 - <<'PY'
import re, pathlib, sys
sys.path.insert(0, "scripts")
from catalog import DOCS
slugs = {d[3] for d in DOCS}
txt = "\n".join(p.read_text(encoding="utf-8")
                for p in pathlib.Path("site-pages/docs").glob("*.html"))
for c in ["NMS", "IoU", "ResNet", "U-Net", "Segmentation", "Optical Flow"]:
    n = len(re.findall(re.escape(c), txt, re.I))
    if n and c.lower().replace(" ", "-") not in slugs:
        print(f"{n:>4}  {c}")
PY
```

---

## 태그로 균형 보기

문서마다 태그가 붙어 있으므로 **어느 쪽이 비었는지 숫자로 보인다.**
주제를 고를 때 이 분포를 먼저 확인한다.

```bash
python3 - <<'PY'
import sys; sys.path.insert(0, "scripts")
from catalog import DOCS, TAGS
from collections import Counter
c = Counter(t for d in DOCS for t in d[7])
for g in ("domain", "topic"):
    print(g)
    for t in TAGS:
        if t["group"] == g:
            print(f"   {t['name']:10} {c[t['id']]:2}편")
PY
```

**2026-08-11 기준 — 관심사 대비 비어 있는 곳**

| 태그 | 편수 | 판단 |
|---|---|---|
| `enhance` 화질 개선 | 3편 | 초해상·denoise·평가지표 완료. ISP 남음 |
| `feature` 특징·매칭 | 6편 | 메트릭러닝·Re-ID·지역특징 완료. 벡터검색·임계값 남음 |
| `on-device` 온디바이스 | 8편 | 경량백본·런타임 완료. NAS 남음 |
| `vision` 도메인 | **21편** (LLM 15편) | 역전됨. 이제 균형이 맞다 |

**고를 때의 원칙**

1. 위 표에서 **0편이거나 관심사 대비 얇은 태그**를 먼저 채운다
2. vision 이 LLM 편수를 따라잡을 때까지 vision 을 우선한다
3. 같은 태그가 연달아 3편 이상 나오지 않게 한다 — 한쪽으로 쏠리면 흐름이 끊긴다

---

## 후보 목록

`언급` = 기존 49편 본문에 등장한 횟수. 많을수록 연결할 곳이 많다는 뜻이다.

### 우선 — vision 기반 (언급 많음)

| 언급 | 주제 | 단계 | 왜 필요한가 | 상태 |
|---|---|---|---|---|
| 33 | ~~NMS & 중복 제거~~ **완료 08-11** | 09 특집 | 검출 문서 6편이 전부 언급하지만 원리·변형(Soft-NMS·NMS-free)을 다룬 곳이 없다 | 대기 |
| 23 | **DINO & 자기증류 시각 표현** | 02 아키텍처 | RF-DETR 백본이자 자기지도 문서에서 언급만 하고 지나간다 | 대기 |
| 14 | ~~IoU 계열 손실~~ **완료 08-18** | 09 특집 | GIoU·DIoU·CIoU 가 검출 성능을 가른 축인데 설명이 없다 | `iou-losses` |
| 9 | ~~SAM & 프롬프트 분할~~ **분할 문서에 포함 08-11** | 09 특집 | 분할 자체가 49편에 없다. vision 쪽 큰 결손 | 대기 |
| 9 | **ResNet & 잔차 연결** | 02 아키텍처 | 거의 모든 문서가 전제하는데 정작 설명이 없다 | 대기 |
| 3 | **U-Net & 인코더-디코더** | 02 아키텍처 | 확산 모델이 쓰는 구조인데 별도 설명이 없다 | 대기 |

### 관심사 — 화질 개선 `enhance` (1편 / 3편 남음)

| 주제 | 단계 | 태그 | 왜 필요한가 |
|---|---|---|---|
| ~~초해상 (Super-Resolution)~~ | 03 학습 | `vision` `enhance` | **완료 2026-08-11** (9b271d6) — 03 학습 단계에 배치 |
| ~~denoise·복원~~ **완료 08-11** | 09 특집 | `vision` `enhance` | 확산 모델이 왜 복원에 잘 맞는지 — 노이즈 제거가 곧 학습 목표였다 |
| ~~화질 평가 지표~~ **완료 08-11** | 08 평가 | `vision` `enhance` `safety` | PSNR·SSIM 이 사람 눈과 어긋나는 지점, LPIPS 가 나온 이유 |
| ~~ISP 와 학습 기반 파이프라인~~ | 09 특집 | `vision` `enhance` `on-device` | **완료 2026-08-11** (ed54c4d) |

### 관심사 — 특징·매칭 `feature` (현재 vision 은 1편)

| 주제 | 단계 | 태그 | 왜 필요한가 |
|---|---|---|---|
| ~~메트릭 러닝~~ **완료 08-11** | 03 학습 | `vision` `feature` | **같은 객체는 가깝게, 다른 객체는 멀게** — 관심사의 핵심. contrastive·triplet·ArcFace |
| ~~재식별 (Re-ID)~~ **완료 08-11** | 09 특집 | `vision` `feature` | 같은 물체를 다른 뷰·다른 카메라에서 같다고 판정하는 문제 그 자체 |
| ~~지역 특징과 매칭~~ **완료 08-11** | 09 특집 | `vision` `feature` | SIFT 에서 SuperPoint·LightGlue 까지. 뷰가 달라도 대응점을 찾는 법 |
| ~~벡터 검색 (ANN)~~ | 07 응용 | `common` `feature` `systems` | **완료 2026-08-11** (ed54c4d) |
| ~~유사도 임계값 정하기~~ | 08 평가 | `vision` `feature` `safety` | **완료 2026-08-11** (ed54c4d) |

### 관심사 — 온디바이스 양자화 `on-device` (2026-08-11 점검 → **3편 전부 완료**)

**현황.** 양자화 문서가 이미 있지만 **LLM 쪽 이야기**다 —
PTQ·GPTQ·AWQ·4비트 가중치가 중심이고, 본문에서 LLM 을 4회 언급한다.
`INT8` `캘리브레이션` `outlier` `NPU` `지연` 은 **한 번도 나오지 않는다**.
반면 온디바이스 vision 배포는 **INT8 활성값 양자화**가 기본이라, 다루는 층위가 다르다.

**가장 좋은 후보 — 기존 문서가 언급만 하고 지나간 것**

`경량 백본` 문서에 이런 문장이 있다:
> "depthwise 층은 채널별 분포 편차가 커서 **채널 단위 양자화가 아니면
> 정확도가 크게 떨어지는 경우**가 알려져 있다."

이 문장이 왜 그런지는 설명하지 않는다. 계산해 보면 이유가 분명하다 —
채널마다 가중치 범위가 크게 다를 때 per-tensor 로 묶으면 이렇게 된다.

```
전체 최대 절댓값 3.2 기준, INT8 대칭 (254단계)
  채널 범위 [-0.02, 0.02] →   1.6 단계만 사용   ← 사실상 뭉개진다
  채널 범위 [-0.05, 0.05] →   4.0 단계
  채널 범위 [-0.80, 0.80] →  63.5 단계
  채널 범위 [-3.20, 3.20] → 254.0 단계
per-channel 로 바꾸면 모든 채널이 254 단계를 온전히 쓴다
```

실측 보고도 일치한다 — MobileNetV2 는 per-tensor W4A8 에서 약 2.5%,
EfficientNet-lite 는 약 4.2% 정확도가 떨어진다.

| 주제 | 단계 | 태그 | 왜 필요한가 | 상태 |
|---|---|---|---|---|
| ~~온디바이스 양자화 (INT8 실전)~~ | 05 추론 | `vision` `on-device` `efficiency` | — | **완료 2026-08-11** (d52f007) |
| ~~캘리브레이션과 활성값 범위~~ | 05 추론 | `vision` `on-device` | — | **완료 2026-08-11** (d52f007) |
| ~~QAT — 학습에 양자화를 넣기~~ | 05 추론 | `common` `on-device` `training` | — | **완료 2026-08-11** (d52f007) |

> **주의 — 겹침 확인 결과.** `quantization`(LLM 중심) · `efficient-backbone`(구조) ·
> `mobile-runtime`(변환·위임) 셋과 층위가 다르므로 신규로 쓸 수 있다.
> 다만 새 문서는 반드시 이 셋과 **양방향 링크**를 걸어야 한다.

### 관심사 — 온디바이스 `on-device` (vision 은 검출에만)

| 주제 | 단계 | 태그 | 왜 필요한가 |
|---|---|---|---|
| ~~경량 백본~~ **완료 08-11** | 02 아키텍처 | `vision` `on-device` `arch` | MobileNet·EfficientNet·ShuffleNet. depthwise separable 이 아끼는 것 |
| **NAS 와 하드웨어 인지 설계** | 02 아키텍처 | `vision` `on-device` | FLOPs 가 아니라 실제 지연을 목표로 구조를 찾는 방식 |
| ~~모바일 추론 런타임~~ **완료 08-11** | 07 응용 | `vision` `on-device` `systems` | TFLite·NCNN·CoreML. 연산자 지원 여부가 모델 선택을 정한다 |

### vision — 아직 언급은 적지만 결손

| 주제 | 단계 | 왜 필요한가 | 상태 |
|---|---|---|---|
| ~~분할~~ **완료 08-11** | 09 특집 | 검출은 6편인데 분할은 0편이다 | 대기 |
| ~~CNN 기초~~ **완료 08-11** | 01 기초 | ViT 문서가 "합성곱 편향"을 논하는데 그 편향의 정체가 설명돼 있지 않다 | 대기 |
| **Optical Flow & 동영상** | 09 특집 | 시간 축이 통째로 빠져 있다 | 대기 |
| **NeRF & 3D 표현** | 09 특집 | 3D 가 없다. Gaussian Splatting 과 묶어도 좋다 | 대기 |
| **ControlNet & 조건부 생성 제어** | 03 학습 | 확산 문서가 CFG 까지만 다룬다 | 대기 |
| **Vision-Language 모델** | 07 응용 | CLIP 이후가 비어 있다. 2026-08-11 뉴스에서 오픈보캐블러리 검출·분할 VLM 리뷰(arXiv:2504.09480)가 나와 근거가 하나 더 쌓였다 — 검출 9편·분할 1편과 이을 곳이 많다 | **대기(우선↑)** |

### 비-vision 결손

| 주제 | 단계 | 왜 필요한가 | 상태 |
|---|---|---|---|
| **Batch Normalization 상세** | 01 기초 | 정규화 문서가 있지만 배치 의존성·추론 동작은 얕다 | 대기 |
| **토크나이저 이후 — 임베딩 검색** | 07 응용 | RAG 문서가 벡터 검색을 전제하는데 설명이 없다 | 대기 |
| **긴 문맥 처리 기법** | 05 추론 | 문맥 확장이 계속 화제인데 다룬 곳이 없다 | 대기 |

---

---

## 2026-08-11 재점검 — 관심사 후보 소진 후

관심사 4개 축(`detection` `on-device` `enhance` `feature`)의 후보를 대부분 썼다.
탐색 스크립트를 다시 돌리니 **언급 횟수가 크게 낮아졌다** —
초기의 NMS(33회)·DINO(23회) 같은 큰 결손은 사라졌다는 뜻이다.

```
 16  Depth        10  Few-Shot      5  NAS
  5  Adversarial   4  Zero-Shot     2  Vision-Language
```

그래서 이제는 **언급 횟수보다 구조적 균형**으로 고르는 편이 낫다.

### 현재 분포 (66편)

| 단계 | 편수 | | 주제 태그 | 편수 |
|---|---:|---|---|---:|
| 01 기초 | 5 | | 학습 | 17 |
| 02 아키텍처 | 8 | | 효율 | 16 |
| 03 학습 | 10 | | 구조 | 13 |
| 04 적응·정렬 | 6 | | 온디바이스 | 12 |
| 05 추론 최적화 | 9 | | 시스템 | 12 |
| **06 학습 인프라** | **3** ← | | 검출·분류 | 9 |
| 07 응용·시스템 | 8 | | 특징·매칭 | 8 |
| 08 평가·안전 | 6 | | 평가·안전 | 7 |
| 09 특집 — 검출 | 11 | | 정렬 | 6 |
| | | | **화질 개선** | **4** ← |
| | | | **생성** | **4** ← |

**얇은 곳 셋** — 06 학습 인프라(3편), `생성`(4편), `화질 개선`(4편)

### 다음 후보 — 얇은 곳부터

| 주제 | 단계 | 태그 | 왜 | 상태 |
|---|---|---|---|---|
| ~~Depth 추정~~ | 09 특집 | `vision` `detection` | — | **완료 08-11** (4925a18) |
| ~~ControlNet & 조건부 생성 제어~~ | 03 학습 | `vision` `generative` `enhance` | — | **완료 08-11** (4925a18) |
| ~~Few-Shot / Zero-Shot 인식~~ | 07 응용 | `vision` `feature` | — | **완료 08-11** (dd61a65) |
| ~~Vision-Language 모델~~ | 07 응용 | `vision` `feature` `systems` | — | **완료 08-11** (dd61a65) |
| ~~데이터 파이프라인·라벨링~~ | 06 인프라 | `common` `systems` `training` | — | **완료 08-11** (4925a18) |
| ~~실험 관리·재현성~~ | 06 인프라 | `common` `systems` | — | **완료 08-11** (dd61a65) |
| ~~NAS 와 하드웨어 인지 설계~~ | 02 아키텍처 | `vision` `on-device` | — | **완료 08-11** (d4c3c4c) |
| ~~적대적 예제와 강건성~~ | 08 평가 | `common` `safety` | — | **완료 08-11** (d4c3c4c) |
| ~~Optical Flow & 동영상~~ | 09 특집 | `vision` `detection` | — | **완료 08-11** (d4c3c4c) |
| ~~NeRF & 3D 표현~~ | 09 특집 | `vision` `arch` | — | **완료 08-11** (d4c3c4c) |

---

## 상태 값

- `대기` — 아직 안 씀
- `작업중` — 오늘 작업 대상
- `완료 YYYY-MM-DD` — 커밋 해시 병기
- `보류` — 사유 병기

---

> **2026-08-11 — 대기열 소진.** 재점검에서 정리한 후보 10건을 전부 썼다.
> 다음 실행은 아래 절차로 목록을 다시 채우는 것부터 시작한다.

---

---

---

---

## 2026-08-13 뉴스 → 기술문서 영향 판정

오늘 뉴스 2건을 기존 **76편**(요청서의 49편은 옛 수치) 기준으로 확인했다.

**갱신 대상: 0건. 대기열 추가: 0건. 문서는 하나도 고치지 않았다.**

| 뉴스 | 관련 문서 | 판정 |
|---|---|---|
| 캘리포니아 AI 법안 보류파일 표결 | 평가·안전 7편 | 입법 일정. 기술 서술과 무관 |
| arXiv 비전 2건 (OPCNet · HNDiff) | `similarity-threshold` · `denoising` | **기존 서술이 이미 같은 논지** — 아래 |

### 왜 갱신이 아닌가

**OPCNet — 위장 물체 검출의 폐쇄세계 가정.**
"모든 이미지에 대상이 있다"는 가정 때문에 음성 표본에서 거짓 양성이 터진다는 지적이다.
`similarity-threshold` 는 이미 **"두 분포는 반드시 겹친다"**를 1절 제목으로 걸고,
어떤 임계값을 골라도 오류가 남는다는 것을 계산과 SVG 로 보인다.
개별 논문 하나가 이 서술을 낡게 만들지 않는다.

**HNDiff — 대기 산란 모델을 확산의 귀납 편향으로.**
`denoising` 은 이미 **"가우시안 노이즈 σ=25 를 더한 데이터는 실제 사진과 성질이 다르다"**고
못박고 포아송+가우시안 혼합을 설명한다. 물리 기반 열화 모델링이라는 방향 자체가
그 문서의 논지와 같다. HNDiff 는 반증이 아니라 실증이다.

> **원칙 재확인.** 새 논문이 나왔다는 사실은 갱신 사유가 아니다.
> **기존 서술이 틀렸을 때만** 고친다. 오늘은 틀리지 않았다.
> 어제 YOLO26 판정과 같은 기준이다.

### 관찰 대상 (대기열 아님)

- **음성 표본이 있는 검출 평가** — COD 뿐 아니라 개방세계 검출 전반의 문제다.
  같은 지적이 몇 번 더 쌓이면 `evaluation-benchmarks` 에 한 절을 붙이는 편이 맞다.
  *새 문서가 아니라 기존 문서 보강 쪽*으로 본다.

---

## 2026-08-12 뉴스 → 기술문서 영향 판정

오늘 뉴스 4건을 기존 **76편**(요청서의 49편은 옛 수치) 기준으로 확인했다.

**갱신 대상: 0건. 문서는 하나도 고치지 않았다.**

| 뉴스 | 관련 문서 | 판정 |
|---|---|---|
| EU AI Act 투명성 집행 | 평가·안전 8편 | 규제 일정이라 기술 서술을 낡게 하지 않음 |
| Jeff Dean → Discovery Loop | 없음 | 인사·산업 |
| 구글 AI 조직 개편 | 없음 | 인사·산업 |
| Fable 5 안전장치 | 없음 | 제품 릴리스 |

### 다만 뉴스와 별개로 확인된 사실 1건

검색 중 **YOLO26**(Ultralytics, 2026-01 공개)의 1차 출처를 열어 확인했다.
24시간 밖이라 뉴스는 아니지만 검출 문서와 직접 닿아 여기 남긴다.

- 기본값이 **NMS-free 단대단**, 출력 `(N, 300, 6)` — 이미지당 최대 300개
- **DFL 제거**로 검출 헤드 단순화
- **MuSGD** — SGD 와 Muon 을 결합한 하이브리드 최적화기
- `YOLO26n` 40.9 mAP / 1.7ms, `YOLO26x` 57.5 mAP / 11.8ms (T4 TensorRT)
- CPU ONNX 에서 YOLO11n 대비 최대 43% 빠름

**그런데 이것이 문서를 낡게 만들지는 않는다.**
`nms.html` 은 이미 "일대다 분기와 일대일 분기를 함께 두고 추론에서는
일대일만 쓴다"는 **구조**를 설명하고, NMS 제거의 동기를 정확도가 아니라
**배포**(동적 연산자·예측 불가능한 지연)로 짚는다.
`mobile-runtime` 도 NMS 를 동적 크기 연산의 대표 사례로 든다.
YOLO26 은 이 서술의 **반증이 아니라 실증**이다.

> 모델 이름을 최신판으로 바꾸는 것은 갱신이 아니다.
> 서술이 틀렸을 때만 고친다 — 지금은 틀리지 않았다.

**대기열에 쌓지 않는다.** 다만 MuSGD(LLM 최적화기의 비전 유입)는
흐름이 더 쌓이면 후보가 될 수 있어 관찰 대상으로만 적어 둔다.

---

## 2026-08-11 2차 재점검 — 76편 시점

대기열 10건을 전부 쓴 뒤 탐색 스크립트를 다시 돌렸다.
**언급 횟수가 사실상 바닥났다** — 최대가 7회(Transfer)이고 나머지는 1~2회다.

```
초기(49편):  NMS 33 · DINO 23 · IoU 14
1차(66편):  Depth 16 · Few-Shot 10 · NAS 5
2차(76편):  Transfer 7 · Curriculum 2 · 나머지 1회
```

**분포도 고르게 찼다.** 단계는 5~14편, 주제 태그는 5~20편 범위이고
관심사 4축은 검출 11 · 온디바이스 13 · 화질개선 5 · 특징매칭 11 이다.
*비어서 급한 곳이 없다.*

### 그래서 다음은 무엇을 하나

**기계적으로 편수를 늘리는 단계는 지났다.** 지금 문서를 더 쓰려면
근거가 "결손"이 아니라 다른 데서 와야 한다. 세 갈래가 있다.

**① 뉴스에서 흐름이 잡힐 때** — 루틴 [A]가 매일 돌고 있다.
같은 주제가 여러 번 쌓이고 6개월 뒤에도 유효해 보이면 그때 쓴다.
지금 대기열이 비어 있는 것은 **정상 상태**이지 채워야 할 구멍이 아니다.

**② 기존 문서를 깊게** — 새 문서 대신 있는 문서에 절을 더한다.
`Transfer`(7회)처럼 여러 곳에서 스치는 개념은
전용 문서보다 관련 문서에 한 절을 붙이는 편이 나을 수 있다.

**③ 사용자가 실제로 필요할 때** — 실무에서 막히는 주제가 생기면
그것이 가장 좋은 근거다. 언급 횟수보다 신뢰할 만하다.

### 그럼에도 쓸 만한 후보 (급하지 않음)

| 주제 | 단계 | 태그 | 근거 |
|---|---|---|---|
| ~~추적 (Tracking)~~ | 09 특집 | `vision` `detection` `feature` | **완료 08-13** — 08-16 회차에서 유실분 복구 |
| 자세 추정 (Pose) | 09 특집 | `vision` `detection` | 분할 문서가 관절 정렬을 언급하고 지나간다 |
| 불확실성·보정 | 08 평가 | `common` `safety` | 임계값 문서가 '점수를 확률로 보정'을 언급만 함 |
| 롱테일·불균형 | 03 학습 | `common` `training` | 데이터 파이프라인이 '불균형'을 전제하고 지나간다 |
| 전이학습 정리 | 04 적응 | `common` `training` | 7회 언급. 다만 ②처럼 기존 문서 보강이 나을 수도 |

---

## 2026-08-16 점검 — 80편 시점

### 이번 회차에서 처리한 것

| 주제 | 단계 | 태그 | 상태 |
|---|---|---|---|
| ~~긴 문맥 (Long Context)~~ | 05 추론 | `llm` `efficiency` `arch` | **신규 08-16** — 아래 근거 |
| ~~DINO 와 자기증류~~ | 03 학습 | `vision` `feature` `training` | **복구 08-16** (원 작성 08-12) |
| ~~다중 객체 추적~~ | 09 특집 | `vision` `detection` `feature` | **복구 08-16** (원 작성 08-13) |
| ~~HDR 과 톤 매핑~~ | 09 특집 | `vision` `enhance` `on-device` | **카탈로그 등록 08-16** (원 작성 08-14) |

**긴 문맥을 고른 근거 — ROUTINE `[B] 1 ①` 에 정확히 해당한다.**

- `flash-attention` 05 절이 <q>긴 문맥이 열렸다</q>고 적고 그대로 끝난다
- `embeddings` 는 위치 보간·NTK·YaRN·ALiBi 를 **한 문단**으로 지나간다
- `rag` 는 <q>긴 문맥이 RAG 를 대체하지 않았다</q>고 쓰는데 왜인지는 설명이 없다
- 셋 다 같은 주제를 가리키는데 전용 문서가 없었다

**태그 균형도 맞는다.** 08-12·08-13·08-14 세 편이 전부 `vision` 이었다 —
ROUTINE `[B]` 의 <q>같은 태그가 연달아 3편 이상 나오지 않게 한다</q>에 걸린다.
`vision` 35편 대 `llm` 15편으로 역전 폭도 커진 상태였다.

### 남은 얇은 곳 (80편 기준)

| 태그 | 편수 | |
|---|---:|---|
| `enhance` 화질 개선 | 6 | ← |
| `generative` 생성 | 6 | ← |
| `alignment` 정렬 | 6 | ← |
| `llm` 도메인 | 16 | `vision` 35 대비 절반 이하 |

> **주의.** 이 목록은 *구멍이라서* 만든 것이 아니라
> *이어서 쓸 것이 필요할 때를 위해* 적어 둔 것이다.
> 억지로 소진할 이유가 없다 — 루틴의 원칙대로
> **쓸 것이 없으면 그렇게 보고하는 것이 정답이다.**

---

## 다 떨어지면

위 목록이 비면 **새로 채운다.** 아래 순서로 찾는다.

1. 위의 탐색 스크립트를 다시 돌린다 (문서가 늘면 새 언급이 생긴다)
2. 최근 뉴스에서 **6개월 뒤에도 유효할 기반 기술**을 고른다
3. 기존 문서를 읽으며 "이 부분은 따로 한 편이 필요하다" 싶은 곳을 표시한다

**후보가 없다고 문서를 대충 쓰지 않는다.** 그럴 땐 사용자에게 알리고 목록 보강을 제안한다.

---

## 2026-08-17 점검 — 81편 시점

### 이번 회차에서 처리한 것

| 주제 | 단계 | 태그 | 상태 |
|---|---|---|---|
| ~~RLVR — 검증 가능한 보상~~ | 04 적응·정렬 | `llm` `alignment` `training` | **신규 08-17** — 아래 근거 |

**RLVR 을 고른 근거 — ROUTINE `[B] 1 ①` 에 해당한다.**

- `rlhf` 05 절이 <q>검증 가능한 과제(수학·코드)에는 규칙 기반 보상으로 강화학습을 돌린다</q>
  <q>최근 추론 모델들이 이 경로를 크게 활용했다</q> 라고 **두 문장 쓰고 넘어간다**
- `chain-of-thought` 04 절도 같은 자리를 <q>정답 여부를 보상으로 쓰면</q> 한 줄로 지나간다
- `knowledge-distillation` 은 <q>최근 소형 추론 모델들이 이 방식으로 만들어졌다</q>고 하는데
  그 교사의 추론 능력이 어디서 왔는지는 비어 있었다
- `GRPO` 는 81편 전체에서 **언급 0회**였다 — 가치망 제거라는 구조 변화가 통째로 없었다

**태그 균형도 맞는다.** `llm` 16편 대 `vision` 35편, `alignment` 6편으로
도메인·주제 양쪽에서 가장 얇은 곳이었다.

### 검토했으나 고르지 않은 것 — 기록해 둔다

| 주제 | 왜 안 골랐나 |
|---|---|
| **라벨 할당 (SimOTA·ATSS·TAL)** | 언급 50회로 후보 중 최다였으나, `yolov5`·`yolox`·`yolo-lineage`·`detr-lineage` 가 **이미 절 단위로 설명하고 있었다**. ROUTINE 원칙 2(겹치면 새로 쓰지 않는다)에 걸린다. |
| **Flow Matching · Rectified Flow** | `generative` 6편으로 얇고 주제 자체는 유효하지만 **기존 81편에서 언급 0회**다. ①(언급됐는데 설명이 없는 것)이 아니라 순수 신규 영역이라 이을 곳이 적다. 쓸 거라면 `diffusion-models` 를 먼저 늘리는 편이 낫다. |
| **IoU 계열 손실 (GIoU·DIoU·CIoU)** | 언급이 `nms` 한 편에 3~4회뿐으로 줄었다. 08-11 시점(49편)의 "14회"는 그 뒤 검출 문서들이 흡수했다. |
| **포즈 추정** | 언급 2회. 이을 곳이 거의 없다. |

### 남은 얇은 곳 (81편 기준)

| 태그 | 편수 | |
|---|---:|---|
| `enhance` 화질 개선 | 6 | 관심사 태그 중 가장 얇다 |
| `generative` 생성 | 6 | ← Flow Matching 이 여기 후보 |
| `alignment` 정렬 | 7 | 이번에 +1 |
| `llm` 도메인 | 17 | `vision` 35 대비 여전히 절반 |

**다음 회차에 볼 만한 것** — 언급 수를 다시 세어 확인할 것.

- `denoising` 이 디블러·모션 블러를 4회 언급하고 지나간다 (`vision` `enhance`)
- `hdr-tone-mapping` 이 멀티 프레임 정합을 7회 쓰는데 전용 편이 없다 (`vision` `enhance` `on-device`)
- `data-pipeline` 이 능동 학습을 3회 언급만 한다 (`common` `training`)

---

## 2026-08-18 회차 — `iou-losses` 선정 근거 (어제 판정을 뒤집었다)

**08-17 회차는 이 주제를 기각했다.** 사유는 <q>언급이 `nms` 한 편에 3~4회뿐으로 줄었다</q>였다.
오늘 다시 세어 보니 **그 계수가 틀렸다.**

```
IoU 를 언급하는 문서: 7편
  nms · object-tracking · rf-detr · segmentation · yolo-lineage · yolov5 · yolox
GIoU 등장: 0회      CIoU 등장: 0회      DIoU 등장: 1회 (nms 의 DIoU-NMS 뿐)
```

즉 **검출 문서 7편이 IoU 를 전제하고 쓰는데, 그것을 손실로 쓰는 이야기는 어디에도 없었다.**
GIoU·CIoU 는 81편 전체에서 단 한 번도 나오지 않았다.
ROUTINE `[B] 1 ①`(언급했지만 설명하지 않은 것)의 정의에 그대로 들어맞는다.

**교훈 — 언급 수를 셀 때 무엇을 세는지 적어 둘 것.**
`grep -c "IoU"` 와 `grep -l "IoU" | wc -l` 은 다른 수를 준다.
기각 사유에 계수 명령을 함께 남기지 않으면 다음 회차가 검증할 수 없다.

### 태그 균형은 이번에도 못 맞췄다

`iou-losses` 는 `vision` `detection` `training` 이다.
가장 얇은 `enhance`(6편)·`generative`(6편)는 그대로 남았다.
결손의 크기(①)와 태그 균형이 어긋날 때 **결손을 우선했다** —
GIoU·CIoU 가 전체에서 0회인 것은 균형 문제가 아니라 구멍이기 때문이다.

**다음 회차는 `enhance` 를 우선할 것.** 08-17 회차가 남긴 후보가 여전히 유효하다.

| 후보 | 태그 | 근거 |
|---|---|---|
| **디블러·모션 블러** | `vision` `enhance` | `denoising` 이 4회 언급하고 지나간다 |
| **멀티 프레임 정합** | `vision` `enhance` `on-device` | `hdr-tone-mapping` 이 7회 쓰는데 전용 편이 없다 |
| **분포 기반 박스 회귀 (DFL)** | `vision` `detection` | `iou-losses` 와 `yolo-lineage` 가 오늘부로 각각 언급만 하고 지나간다 ← **오늘 새로 생긴 결손** |
