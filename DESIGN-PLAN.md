# 공개 사이트 리디자인 계획

> **기록용 문서.** 당시의 계획과 판단을 남긴 것입니다.
> 절차는 여기가 아니라 [ROUTINE.md](ROUTINE.md) 를 따르세요 — 게임 서버·Tailscale 등 지금은 쓰지 않는 경로가 언급될 수 있습니다.

## 왜 고치는가

지금 목록 페이지의 CSS는 **게임 허브에서 그대로 복사한 것**이다(`style.css` 첫 줄 주석이 그렇게 적고 있다).
네온 그라데이션 제목, 보라-파랑 radial 배경, 전부 16px 라운드 — 게임 카드에는 맞지만
AI 기술 문서에는 맞지 않는다.

결정적인 문제는 **문서 본문과의 불일치**다. `docs/*.html`은 차분한 편집 디자인(세리프 제목, 절제된 팔레트,
라이트/다크 양쪽 지원)인데 목록만 게임풍이라, 링크를 누르는 순간 다른 사이트로 넘어간 느낌이 든다.

또 현재 디자인은 `artifact-design`이 "피하라"고 명시한 패턴에 정확히 해당한다 —
무지개 그라데이션 텍스트, 보라-파랑 그라데이션 히어로, 일괄 rounded + 큰 그림자.

---

## 설계 방향

**주제에서 출발한다.** 이 사이트의 내용은 *모델이 만들어져 서비스되기까지의 순서*다.
파이프라인·계보·단계. 그렇다면 시각적 은유는 **공정 도면이나 인쇄된 기술 색인**이지
게임 런처가 아니다.

문서 본문이 이미 그 방향(신문·저널 편집 디자인)이므로, 목록을 거기에 맞춘다.
사이트 전체가 하나의 출판물처럼 읽히는 것이 목표다.

### 색 — 종이와 잉크, 그리고 한 점의 강조

브랜드 없는 무채색을 쓰되 **완전 중립은 피한다**(고른 회색은 "고르지 않은 것"으로 읽힌다).
청록 쪽으로 아주 살짝 기운 회색을 바탕으로 쓰고, 강조는 **단 하나**만 둔다.

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| `--paper` | `#f4f4f2` | `#101211` | 바탕 |
| `--panel` | `#eaeae7` | `#181b1a` | 카드·구분 영역 |
| `--ink` | `#14181a` | `#e6eae8` | 본문 |
| `--ink-soft` | `#4d5654` | `#9fa9a6` | 보조 텍스트 |
| `--ink-faint` | `#7d8785` | `#6b7573` | 메타 정보 |
| `--rule` | `#d5d7d3` | `#242927` | 괘선 |
| `--accent` | `#0f6f63` | `#3fc9b4` | 단계 번호·링크·호버 (딥 틸) |

무지개 그라데이션은 전부 제거한다. 강조색은 **단계 번호와 상호작용에만** 쓴다.

### 타이포그래피 — 두 개의 역할

| 역할 | 서체 | 용도 |
|---|---|---|
| 표제 | Charter / Iowan Old Style / Georgia (세리프) | h1, 단계명, 문서 제목 |
| 본문 | Pretendard / Apple SD Gothic Neo / Noto Sans KR | 설명·요약 |
| 데이터 | ui-monospace | 번호, 편수, 날짜 |

문서 본문이 쓰는 것과 같은 스택이다. 웹폰트를 새로 불러오지 않으므로
44KB 유지 + CSP 문제 없음.

`letter-spacing: 3px`는 제거한다 — 한글에서 가독성을 떨어뜨린다.

### 레이아웃 — 색인의 구조

- 그라데이션 히어로 제거. 제목 + 한 줄 설명 + 얇은 괘선.
- 단계는 **번호를 왼쪽 여백에 세우고** 본문을 오른쪽에 두는 2열 구조.
  파이프라인의 순서를 시각적으로 드러낸다.
- 문서 항목은 카드(박스)가 아니라 **괘선으로 구분된 행**.
  26편을 훑을 때 박스 26개보다 목록이 스캔하기 쉽다.
- 호버는 크게 움직이지 않는다 — 배경이 아주 살짝 들어오고 화살표가 2px 이동.

### 접근성

- 라이트/다크 양쪽 지원 (`prefers-color-scheme` + `[data-theme]` 오버라이드)
- 포커스 표시 유지 (`:focus-visible`)
- `prefers-reduced-motion` 존중
- 본문 대비 WCAG AA 이상

---

## 적용 범위

| 대상 | 작업 |
|---|---|
| `site-pages/index.html` | 구조 정리, 폰트 스택 |
| `site-pages/style.css` | **전면 교체** |
| `site-pages/ai.js` | 렌더링 마크업을 새 구조에 맞춤 |
| `site-pages/docs/*.html` | 상단 네비만 새 팔레트로 (본문은 유지) |
| 집 서버 `/ai/` | **건드리지 않음** — 게임 허브 안에서는 지금 톤이 맞다 |

집 서버와 공개 사이트가 갈라지므로, `sync-catalog.sh`는 데이터(JSON)만 복사하고
CSS/JS는 각자 유지하도록 이미 분리되어 있다.

---

## 검증

- [ ] 라이트/다크 양쪽에서 대비 확인
- [ ] 좁은 화면(360px)에서 2열 붕괴 확인
- [ ] 문서 페이지로 이동했을 때 톤이 이어지는지
- [ ] 44KB 수준 유지 (외부 폰트·CDN 없음)
