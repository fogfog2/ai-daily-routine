# AI Daily — 2026-08-17

> **오늘자로 확인된 항목이 1건이다.** 숫자를 채우지 않았다.
>
> **어제와 같은 시각 문제다.** 이 회차도 **00:0x UTC** 에 돌았다.
> "오늘"이 시작된 지 몇 분째라 오늘 날짜로 공개된 것이 사실상 없다.
> 검색에 걸린 것은 대부분 08-10 ~ 08-15 사건이었다 —
> Meta Muse Spark 1.2 가중치 공개, NVIDIA Nemotron 3.5 Lightning,
> Gemini 3.7 Flash(08-13), BenchLM 8월 순위 갱신(08-05) 등.
> ROUTINE `[A] 1` 의 "어제 일어난 사건은 오늘 끌어오지 않는다" 에 따라 싣지 않았다.
>
> **arXiv 는 이번에도 쓰지 못했다.** 2026-08-17 은 월요일이라
> 00:00 UTC 공고가 실제로 있었지만, `arxiv.org` 와 `export.arxiv.org` 가
> 이 실행 환경의 이그레스 프록시에서 차단돼 목록을 열 수 없었다.
> 08-16 회차(일요일, 공고 자체가 없음)와는 원인이 다르다 —
> 이쪽은 **접근 차단**이다.
>
> **확인 실패.** 아래 항목의 1차 출처인 `ai.google.dev` · `firebase.google.com` 도
> 전부 차단돼 있어 원문을 열지 못했다. `(미확인)` 으로 표시했다.
> 다만 GitHub 은 열려서 Google 자체 SDK 저장소로 모델 ID 존재는 대조했다.

## Google, Imagen 4 생성 엔드포인트 3종을 오늘자로 종료 (미확인)

- 분류: `도구`
- 출처: https://ai.google.dev/gemini-api/docs/models/imagen

Gemini API 에서 `imagen-4.0-generate-001`, `imagen-4.0-ultra-generate-001`,
`imagen-4.0-fast-generate-001` 세 모델이 **2026-08-17 자로 종료**된다.
Vertex AI 쪽 사용 중단 고지는 2026-03-24 에 나왔고, Gemini API 종료일이 오늘이다.
권장 대체 모델은 `gemini-3.1-flash-image` 다.
호출 형태가 바뀌므로 드롭인 교체가 아니다 —
`client.models.generate_images()` 가 사라지고,
이미지는 `response.generated_images` 배열이 아니라
`response.candidates[0].content.parts` 안의 콘텐츠 파트로 온다.
`number_of_images` 파라미터도 없어져 호출당 이미지 1장이 된다.

**확인 범위.** 1차 출처(`ai.google.dev`, `firebase.google.com`)는 이그레스 차단으로 열지 못했다.
날짜·모델 ID·대체 모델은 서로 독립인 2차 출처 3곳이 일치했고,
모델 ID 자체는 Google 의 `googleapis/python-genai` 저장소 문서에서 확인했다
(다만 그 저장소에는 사용 중단 표시가 아직 없다).
**종료가 실제로 오늘 집행됐는지는 확인하지 못했다.**

그래서: 신규성은 없다 — 5개월 전 고지된 일정이 오늘 만료될 뿐이다. 그래도 실은 이유는 오늘 코드가 깨지는 쪽이 있어서다. 응답 구조와 장당 호출 수가 함께 바뀌므로 모델 이름만 갈아 끼우면 되는 종류가 아니다.
