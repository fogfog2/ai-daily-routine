# AI Daily — 2026-08-18

> **오늘자로 확인된 항목이 1건이다.** 숫자를 채우지 않았다.
>
> **사흘째 같은 시각 문제다.** 이 회차도 **00:0x UTC** 에 돌았다.
> "오늘"이 시작된 지 몇 분째라 오늘 날짜로 공개된 것이 사실상 없다.
> 검색에 걸린 것은 대부분 08-10 ~ 08-16 사건이었다 —
> Meta Muse Glimmer(08-16 회차에 이미 실림), transformers v5.15.0(08-10),
> GLM-5.3(08-14), Kimi K3 등. ROUTINE `[A] 0·1` 의
> 중복 금지와 "어제 일어난 사건은 오늘 끌어오지 않는다" 에 따라 싣지 않았다.
>
> **arXiv 는 사흘 연속 쓰지 못했다.** 2026-08-18 은 화요일이라 00:00 UTC 공고가
> 있었을 것이나, `arxiv.org` 가 이 실행 환경의 이그레스 프록시에서 차단돼 목록을 열 수 없었다.
>
> **확인 실패 — 원인이 구조적이다.** 이 환경의 프록시는 사실상
> `github.com` 과 패키지 레지스트리 외의 도메인을 전부 막는다.
> 이번 회차에서 열지 못한 곳: `arxiv.org`, `learn.microsoft.com`,
> `support.microsoft.com`, `techcrunch.com`.
> WebSearch(검색 결과 요약)는 되지만 **원문 열람이 안 되므로**
> ROUTINE `[A] 3` 의 "원문을 연다"를 이 환경에서는 지킬 수 없다.
> 아래 항목을 `(미확인)` 으로 표시한 이유다.

## Microsoft, Copilot 앱을 하나로 합치며 소비자 기능 3종을 오늘자로 종료 (미확인)

- 분류: `도구`
- 출처: https://support.microsoft.com/en-us/microsoft-365-copilot/learning/changes-microsoft-copilot-app

소비자용 Copilot 과 Microsoft 365 Copilot 두 앱이 하나로 합쳐지고,
**2026-08-18 자로 기능 3종이 종료**된다 — Podcasts(문서·웹페이지를 오디오 대담으로 만들던 기능),
Group Chat(스레드·메시지·이미지 포함 전체), Deep Research(웹 출처를 인용한 장문 리포트 생성).
음성 모드의 애니메이션 캐릭터 Mico 도 핵심 소비자 경험에서 빠진다.
Deep Research 는 삭제가 아니라 **무료 등급에서 제거**다 —
기존 리포트는 대화 기록에 남지만 새로 만들려면 월 19.99 달러의
Microsoft 365 Premium 이 필요하고, 후속 기능인 Researcher 도 같은 유료 등급에만 열린다.
웹 앱 주소가 `m365.cloud.microsoft` 에서 `copilot.cloud.microsoft` 로 바뀌므로
조직 방화벽·프록시 허용 목록에 `*.cloud.microsoft` 를 넣어야 한다.
모바일·웹은 8월 중순부터 전역 배포, Windows·Mac 데스크톱은 9월 중순부터다.

**확인 범위.** 1차 출처(`support.microsoft.com`, `learn.microsoft.com`)는 이그레스 차단으로 열지 못했다.
날짜·기능 목록·가격·주소 변경은 서로 독립인 2차 출처 여러 곳(TechRepublic·TechCrunch·
GeekWire·Windows Central·Neowin)이 일치했으나, **원문을 대조하지 못했고
종료가 실제로 오늘 집행됐는지도 확인하지 못했다.**

그래서: 신규성은 없다 — 08-13 에 고지된 일정이 오늘 발효될 뿐이다. 그래도 싣는 이유는 어제의 Imagen 4 건과 같다. 기능이 사라지는 날과 과금 경계가 바뀌는 날이 오늘이고, 도메인이 바뀌므로 사내 프록시를 쓰는 쪽은 오늘 접속이 끊긴다. 한편 무료 등급에서 심층 리서치를 걷어 유료로 옮긴 것은 에이전트형 기능의 원가가 광고·번들로 감당되지 않는다는 신호에 가깝다.
