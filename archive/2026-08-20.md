# AI Daily — 2026-08-20

> **오늘자로 확인된 항목이 없다. 0건이다.** 숫자를 채우지 않았다.
>
> **닷새째 같은 시각 문제다.** 이 회차도 **00:0x UTC** 에 시작했다.
> "오늘"(2026-08-20)이 시작된 지 몇 분째라, 이 날짜로 공개된 1차 사실이 사실상 없다.
> 08-16 · 08-17 · 08-18 · 08-19 회차가 각각 0 · 1 · 1 · 0 건에 그친 것과 같은 원인이다.
> **루틴의 실행 시각을 옮기지 않는 한 이 수치는 나아지지 않는다.**
> 권장 스케줄(`CLOUD-SETUP.md`)은 09:37 인데 실제로는 00:0x 에 돈다.
>
> **검색으로 걸린 것은 전부 08-02 ~ 08-17 사건이었다** —
> GLM-5.3(08-14) · Qwen3.8-27B(08-14) · Gemini 3.7 Flash(08-13) 등 모델 공개,
> 개방형 가중치 규제를 둘러싼 업계 공개서한(07-24 발단, 이후 지속),
> EU AI Act 의 GPAI 집행 권한 발효(08-02), Kimi K3(문샷) 벤치마크 보도(08-17 전후),
> IJCAI-ECAI 2026 회기(08-15~21, 진행 중이나 08-20 자 개별 발표는 잡히지 않음).
> ROUTINE `[A] 0·1` 의 중복 금지와 "어제 일어난 사건은 오늘 끌어오지 않는다" 에 따라 싣지 않았다.
> 이 중 어느 것도 오늘 처음 공개된 것이 아니다.
>
> **확인 실패 — 원인은 08-18·08-19 회차에 적은 것과 같고 그대로다.**
> 이번 회차에서 직접 재확인: `arxiv.org` 에 대한 WebFetch 가
> `EGRESS_BLOCKED — Access to arxiv.org is blocked by the network egress proxy` 로 거부됐다.
> 프록시의 `noProxy` 목록은 `github.com` 과 패키지 레지스트리(npm·pypi·crates·go)만 열어 둔다.
> WebSearch(검색 결과 요약)는 되지만 **원문 열람이 안 되므로**
> ROUTINE `[A] 3` 의 "원문을 연다"를 이 환경에서 지킬 수 없다.
> 열지 못한 것을 `(미확인)` 으로 실어 건수를 채우는 대신 싣지 않는 쪽을 골랐다.
>
> **검색 각도는 6개를 돌렸다** — 종합/모델 공개 · 비전 · 로보틱스·체화 ·
> 규제/개방 가중치 · 벤치마크/논문 · 회사별. 어느 축에서도 오늘자 1차 사실이 나오지 않았다.
>
> 문서 쪽 `[B]` 는 정상 수행했다 — `deblurring`(디블러링) 1편 신규(84편).
