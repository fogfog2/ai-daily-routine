__pycache__/
*.pyc

# 공개 사이트는 별도 저장소(fogfog2/ai-concepts)다.
# ship-docs.sh 가 필요할 때 clone 하므로 여기 담지 않는다.
site-pages/
# 심볼릭 링크로 연결해 쓰는 경우도 있다 — 슬래시 붙은 패턴은 링크를 잡지 못한다
site-pages
site-pages.bak.*

# 로컬 빌드 산출물
site/
